# /Users/idukbook/git/invoice-app-backend/.env.example
# App
NODE_ENV=development
PORT=3333

# MongoDB
# Desarrollo usa siempre localhost automáticamente.
# Producción usa MONGODB_URI o el valor por defecto del docker compose.
# Para producción, establece MONGODB_URI explícitamente.
# MONGODB_URI=mongodb://admin:secret@mongodb:27017/invoice-app?authSource=admin

# JWT
JWT_SECRET=change_me_super_secret

# SMTP (para nodemailer)
SMTP_SERVICE=gmail
SMTP_USER=invoiceappserver@gmail.com
SMTP_PASS=change_me_app_password

# Stripe
STRIPE_PUBLIC_KEY=pk_test_change_me
STRIPE_SECRET_KEY=sk_test_change_me